// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['najma.cloud'], // Replace with your domain
    },
  };
  
  module.exports = nextConfig;
  